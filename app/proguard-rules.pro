# Minification is disabled by default for this app (see build.gradle.kts).
# If you enable it later, keep Room entities so reflection-based mapping still works:
-keep class com.stockmanager.app.data.** { *; }
